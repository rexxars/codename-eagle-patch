# BuildKit per-Dockerfile ignore; paths are relative to the build context
# (the repo root - see docker-compose.yml / the build command in README.md).
.git
.gitattributes
.gitignore
installers
patch
ripmusic
docs
scripts
docker-dedicated
!docker-dedicated/entrypoint.sh
**/.DS_Store
